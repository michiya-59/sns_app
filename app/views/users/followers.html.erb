<div class="user_show_main">
  <%= render 'sns_profile_part' %>
  <div class="user_show_main_right">
    <p class="follower_or_following_msg">フォロワー<p>
    <% if @users.present? %>
      <%= render 'follow_follower_list', { users: @users } %>
    <% else %>
      <p class="zero_msg">誰にもフォローされていません</p>
    <% end %>
  </div>
</div>